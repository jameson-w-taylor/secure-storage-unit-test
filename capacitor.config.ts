import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'secure-storage-unit-test',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
